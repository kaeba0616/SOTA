[project]
name = "sota"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["playwright>=1.44", "requests>=2.31", "pillow>=10.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
